import { createBrowserRouter } from 'react-router-dom';

import APPLICATION_ROUTES from './appRoutes';

const APPLICATION_ROUTER = createBrowserRouter(APPLICATION_ROUTES);

export default APPLICATION_ROUTER;
